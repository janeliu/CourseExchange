<h2> New Course </h2>
<%= simple_form_for @course do |f| %> 
  <%= f.input :name %>
  <%= f.input :SLN %>
  <%= f.association :registrations, label_method: :kind %>
  <%= f.button :submit %>

  
<% end %>

